similar to reviewers, new expandable entity which is a file, use the file service to do the same things.

First layer test of service layer, even without expansion.
Expansion can be done afterwards.

